import { Series, Volume, UserStats } from './types';

export const USER_STATS: UserStats = {
  totalVolumes: 0,
  activeSeries: 0,
  completionRate: 0,
  estimatedValue: 0,
  monthlyGrowth: 0
};

export const MOCK_SERIES: Series[] = [];

export const MOCK_VOLUMES: Volume[] = [];